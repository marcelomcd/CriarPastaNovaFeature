# =============================================================================
# Modelo de variáveis de ambiente (copie para .env e preencha os valores).
# O arquivo .env NÃO é enviado ao Azure Repos; use estes nomes e valores
# ao configurar as variáveis em Pipelines → Variables no Azure DevOps.
# Documentação: docs/CONFIGURAR_PIPELINE.md e docs/PIPELINES_RETRY_E_CONSOLIDAR.md
# =============================================================================

# -----------------------------------------------------------------------------
# Pipeline PRINCIPAL (azure-pipelines.yml) — Varredura Features + pastas SharePoint
# -----------------------------------------------------------------------------

# Azure DevOps (obrigatório para a pipeline principal)
AZURE_DEVOPS_ORG=qualiit
AZURE_DEVOPS_PROJECT=Quali IT - Inovação e Tecnologia
AZURE_DEVOPS_PAT=seu_pat_aqui

# SharePoint / Microsoft Entra ID (obrigatório para as duas pipelines)
SHAREPOINT_SITE_URL=https://qualiitcombr.sharepoint.com/sites/projetosqualiit
SHAREPOINT_CLIENT_ID=seu_client_id
SHAREPOINT_CLIENT_SECRET=seu_client_secret
SHAREPOINT_TENANT_ID=seu_tenant_id

# Pasta base na biblioteca (não incluir nome da biblioteca para não duplicar)
SHAREPOINT_FOLDER_PATH_BASE=Projetos DevOps

# Opcionais — Pipeline principal
# LOG_LEVEL=INFO
# PIPELINE_FULL_SCAN=0
# PIPELINE_ONLY_CLOSED=0

# -----------------------------------------------------------------------------
# Pipeline CONSOLIDAR (azure-pipelines-consolidate.yml) — Unificar pastas no SharePoint
# -----------------------------------------------------------------------------
# Além das variáveis SharePoint acima, defina UMA das duas opções de origem:

# Opção 1 (recomendado): caminhos na biblioteca, separados por ;
SHAREPOINT_SOURCE_FOLDER_PATHS=Documentação dos Clientes;Documentação dos Projetos;Projetos DevOps OLD;TestePowerAutomate

# Opção 2: links de compartilhamento (/:f:/s/...?e=xxx), separados por ;
# SHAREPOINT_SOURCE_FOLDER_URLS=https://....sharepoint.com/:f:/s/...?e=xxx;https://....

# Opcionais — Pipeline Consolidar
# LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Outros (opcional)
# -----------------------------------------------------------------------------
# Webhook (Service Hooks Azure DevOps → POST /webhook/devops)
# WEBHOOK_SECRET=um_secret_forte_para_validar_webhooks
